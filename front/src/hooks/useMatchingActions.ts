// export const matchingActions = (schedule, me) => {
//   const { isPermitted, permission, start, Cancel } = schedule;
//   const isLast = compareAsc(start, new Date()) < 0;
//   if (isLast) return [] as const;
//   if (permission && !isLast) {
//     if (!Cancel?.id) {
//     } else {

//     }
//   }
//   return [] as const;
// };
